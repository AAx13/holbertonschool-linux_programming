/**
 * _strpbrk - search a string for any of a set of bytes.
 * @s: A string.
 * @accept: A string.
 *
 * Return: Pointer to the byte in s that matches one of the bytes in accept
 * Or NULL.
 */
char *_strpbrk(char *s __attribute__((unused)),
	       char *accept __attribute__((unused)))
{
	return (0);
}
